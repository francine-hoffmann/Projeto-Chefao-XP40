

export default function Bolinhas(props) {
    return (
        <main>
             <h1> {props.titulo} </h1>
                <section>
                    <a href="#" > {props.texto} </a>
                </section>
        </main>
    );
}